这是一个全栈开发支持多网站建设的系统平台，通过配置沉淀重复性工作并提供定制化需求

`pnpm install` 安装依赖

`pnpm build` 打包项目

`node ./dist/app/webpack/build.js` 使用 webpack 打包项目

`pnpm dev` 以开发环境启动项目

`pnpm beta` 以测试环境启动项目

`pnpm production` 以生产环境启动项目