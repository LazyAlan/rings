这是一个全栈开发支持多网站建设的系统平台，通过配置沉淀重复性工作并提供定制化需求

`pnpm install` 安装依赖

`pnpm build:dev` 以开发环境编译项目同时执行 webpack 打包

`pnpm build:prod` 以生产环境编译项目同时执行 webpack 打包

`pnpm dev` 以开发环境启动项目

`pnpm beta` 以测试环境启动项目

`pnpm production` 以生产环境启动项目